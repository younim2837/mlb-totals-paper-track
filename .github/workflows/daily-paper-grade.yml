name: Daily Paper Grade

on:
  workflow_dispatch:
    inputs:
      season:
        description: "Season year to rebuild. Leave blank for current Pacific year."
        required: false
        type: string

permissions:
  contents: write

jobs:
  grade-paper-track:
    runs-on: ubuntu-latest
    env:
      GRADE_SEASON: ${{ inputs.season }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt

      - name: Refresh completed games
        run: python collect_games.py

      - name: Refresh team batting stats
        run: python collect_team_batting.py --update-current

      - name: Refresh pitcher stats
        run: python collect_pitcher_stats.py --update-current

      - name: Refresh bullpen usage logs
        run: python collect_bullpen_usage.py --update-current

      - name: Refresh umpire game log
        run: python collect_umpires.py --update

      - name: Refresh lineup features
        run: python collect_team_lineups.py --update-current

      - name: Refresh historical Kalshi replay
        shell: bash
        run: |
          season="${GRADE_SEASON}"
          if [ -z "${season}" ]; then
            season="$(TZ=America/Los_Angeles date +%Y)"
          fi
          python collect_kalshi_historical.py --season "${season}" --resume
          python simulate_2026_season.py --season "${season}"

      - name: Rebuild paper trackers
        shell: bash
        run: |
          season="${GRADE_SEASON}"
          if [ -z "${season}" ]; then
            season="$(TZ=America/Los_Angeles date +%Y)"
          fi
          python grade_paper_tracking.py --season "${season}"
          python build_dashboard.py --season "${season}"

      - name: Commit graded outputs
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data paper_tracking docs
          if git diff --cached --quiet; then
            echo "No graded changes to commit."
            exit 0
          fi
          season="${GRADE_SEASON}"
          if [ -z "${season}" ]; then
            season="$(TZ=America/Los_Angeles date +%Y)"
          fi
          git commit -m "Automated paper-track grading for ${season}"
          git push
